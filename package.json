{
  "name": "alexandrea",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-root tooling (Husky, commitlint, gitleaks) for Alexandrea. Frontend lives in frontend/; backend lives in backend/ (Java 25 + Spring Boot 4 + Gradle).",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
