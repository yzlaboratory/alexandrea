{
  "name": "entertainment-library",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-root tooling (Husky, commitlint, gitleaks). Frontend lives in frontend/; backend will live in backend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
